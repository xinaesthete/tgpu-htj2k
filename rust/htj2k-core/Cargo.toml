[package]
name = "htj2k-core"
version = "0.0.0"
edition = "2021"
description = "CPU core for an HTJ2K codec: codestream parsing and HT block decoding."
license = "BSD-2-Clause"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
